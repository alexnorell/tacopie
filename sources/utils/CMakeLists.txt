target_sources(tacopie
	PRIVATE
		error.cpp;
		error.hpp;
		logger.cpp;
		logger.hpp;
		thread_pool.cpp;
		thread_pool.hpp;
		typedefs.hpp;
)
target_include_directories(tacopie
	PRIVATE
		${CMAKE_CURRENT_SOURCE_DIR}
)